# pip install -r requirements.txt
flask>=3.0
numpy>=1.26
psutil>=5.9          # monitor: CPU/RAM telemetry

# engines
openmm>=8.5          # OpenMM track (implicit-solvent peptide MD)
pyscf>=2.13          # QM/QM-MM track (HF/DFT + MM electrostatic embedding)
ase>=3.26            # optimiser + PySCF calculator glue

# Martini coarse-grained bilayer builder.
# insane imports pkg_resources, which setuptools >=81 removed — hence the pin.
setuptools<81
insane>=1.2

# NOT required for the deterministic plan pipeline:
#   anthropic  — only if you want the cloud LLM backend instead of local Ollama
# 3Dmol.js is vendored in viewer/static/. GROMACS is external (see SETUP.md).
